# Server Configuration
# The port the backend server will run on
PORT=3003

# The host/IP address the server will bind to
# Use 0.0.0.0 to bind to all network interfaces
# Use 127.0.0.1 or localhost to only allow local connections
# Use your specific IP (e.g., 192.168.1.100) to bind to that interface
HOST=0.0.0.0

# Frontend Configuration (Vite dev server)
# The port the frontend development server will run on
VITE_PORT=5173

# The host/IP address for the Vite dev server
VITE_HOST=0.0.0.0
